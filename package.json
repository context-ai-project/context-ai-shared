{
  "name": "@context-ai/shared",
  "version": "0.1.0",
  "description": "Shared types, DTOs and validators for Context.ai project",
  "private": false,
  "//": "NOTA: 'private: false' y 'access: public' solo para propósitos académicos. En producción cambiar a private: true o access: restricted",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "prepare": "pnpm build",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage"
  },
  "keywords": [
    "context-ai",
    "shared",
    "dto",
    "types"
  ],
  "author": "Context.ai Team",
  "license": "MIT",
  "dependencies": {
    "class-validator": "^0.14.1",
    "class-transformer": "^0.5.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.5",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.11",
    "ts-jest": "^29.1.1"
  },
  "publishConfig": {
    "@context-ai:registry": "https://npm.pkg.github.com",
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gromeroalfonso/context-ai-shared.git"
  }
}

